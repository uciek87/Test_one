name: First Action

on:
  workflow_dispatch: 

jobs:
  drawtree:
    runs-on: windows-latest

    steps:
    
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Run PowerShell tree script
    shell: pwsh
      run: ./tree.ps1
