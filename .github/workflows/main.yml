name: First Action

on:
draw-tree:
  workflow_dispatch: 

jobs:
  drawtree:
    runs-on: windows-latest

    steps:
    - name: Run PowerShell tree script
    shell: pwsh
      run: ./workflows/tree.ps1
